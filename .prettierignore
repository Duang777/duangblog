# Ignore everything
/*

# Except these files & folders
!/src
!/public
!/.github
!tsconfig.json
!astro.config.ts
!package.json
!.prettierrc
!eslint.config.js
!README.md
!.vscode

# Ignore MDX files
*.mdx

# Article prose is hand-set. Prettier pads Markdown tables out with spaces and
# strips the trailing double spaces the posts use as hard line breaks, so
# formatting the content rewrites published articles rather than tidying code.
# Everything here is prose — no code lives under src/content.
src/content/**